# Chrome Web Store 限定公開（Unlisted）の登録手順と記載内容

デベロッパーダッシュボードの各入力欄に、そのままコピーして使える内容をまとめてあります。

- ダッシュボード: <https://chrome.google.com/webstore/devconsole/>
- 初回のみ開発者登録料 **$5**（クレジットカード）
- **限定公開でも審査は必要**です。この拡張機能は外部通信・リモートコード・データ収集がないため、審査で問題になる要素はありません

## 事前に決めること: 限定公開 と 非公開 の違い

| 公開設定 | 誰がインストールできるか |
| --- | --- |
| **限定公開（Unlisted）** | URL を知っている人は誰でも。検索・カテゴリには出ない |
| **非公開（Private）** | 同じ Google Workspace ドメインのユーザーのみ。Workspace アカウントで開発者登録した場合に選択できる |

社外にリンクが漏れてもインストールされたくない場合は、自社ドメインの Workspace アカウントで登録して **非公開** を選ぶ方が目的に合います。手順と記載内容はどちらでも同じです。

## 1. パッケージを作る

```bash
npm run pack     # github-auto-viewed.zip を作成（manifest.json / src / icons のみ）
```

更新するたびに `manifest.json` の `version` を上げてから再作成します（例: `1.0.0` → `1.0.1`）。同じバージョンは再アップロードできません。

## 2. ストアの掲載情報

### アイテム名

```
GitHub Auto Viewed
```

### 概要（132 文字以内 / manifest の description が初期値になります）

```
GitHub の Pull Request で、test や spec などのキーワードに一致するファイルを自動的に Viewed にして折りたたみます。
```

### 説明

```
GitHub の Pull Request の「Files changed」で、テストファイルやスナップショット、ロックファイルなど、レビューで毎回そのまま流し読みするファイルを自動的に Viewed にして折りたたみます。差分の本質的な変更だけが画面に残るので、レビューの見通しが良くなります。

■ できること
・キーワードに一致したファイルを自動で Viewed にして折りたたむ
・キーワードごとにチェックボックスで ON / OFF を切り替え
・自由なキーワードの追加・削除（ロックファイルや自動生成ファイル向けのプリセットも用意）
・ツールバーのアイコンから、現在のページの対象ファイル数の確認と手動実行

■ 既定で有効なキーワード
test / spec / .spec
（大文字小文字は区別しません）

■ 既定で無効なキーワード（チェックを入れるとすぐ使えます）
/tests/ ・ /__tests__/ ・ __mocks__ ・ fixture ・ package-lock.json ・ yarn.lock ・ pnpm-lock.yaml

■ キーワードの指定方法
・部分一致と正規表現を選べます
・照合対象はパス全体とファイル名のみを選べます
・「/tests/」のようにスラッシュを含めるとディレクトリを指定できます。パスは先頭にスラッシュを付けた形（/src/user.test.ts）で照合するため、リポジトリ直下の tests/ にも一致します
・設定画面のプレビューで、任意のパスがどう判定されるか確認できます

■ 注意
・Viewed を外す操作は行いません（付けるだけの片方向）。すでに Viewed のファイルには触りません
・github.com のみで動作します

■ プライバシー
外部サーバーへの通信は一切行いません。閲覧内容やファイル名を収集・送信することはありません。設定内容は Chrome の同期ストレージ（chrome.storage.sync）にのみ保存されます。
```

### カテゴリ / 言語

- カテゴリ: **デベロッパー ツール**
- 言語: **日本語**

### 画像

| 用途 | サイズ | 状態 |
| --- | --- | --- |
| アイコン | 128x128 | `icons/icon128.png` をそのまま使えます |
| スクリーンショット（1 枚以上必須） | 1280x800 または 640x400 | 撮影が必要（下記参照） |
| 小さなプロモーション タイル | 440x280 | 任意。限定公開なら不要 |

スクリーンショットは、拡張機能を読み込んだ状態で撮るのが確実です。

1. `chrome://extensions` から読み込む（README のインストール手順）
2. Pull Request の Files changed を開き、テストファイルが折りたたまれた状態にする
3. ツールバーのアイコンをクリックしてポップアップを開く
4. `Cmd+Shift+4` → `Space` でウィンドウを撮影
5. 規定サイズに整える:

```bash
tools/make-store-screenshot.sh ~/Desktop/撮影した画像.png store-screenshot-1.png
```

設定画面（拡張機能のオプション）も 1 枚あると、キーワード設定の様子が伝わります。

## 3. プライバシーの設定タブ

### 単一目的の説明

```
GitHub の Pull Request の Files changed 画面で、ユーザーが設定したキーワードに一致するファイルの Viewed チェックボックスを自動的にオンにし、その差分表示を折りたたむこと。
```

### 権限の使用理由

| 権限 | 記載内容 |
| --- | --- |
| `storage` | ユーザーが設定したキーワードと動作設定（自動実行の有無、折りたたみの有無など）を保存し、Chrome の同期機能で本人の端末間で共有するために使用します。 |
| `activeTab` | ツールバーのポップアップで「今すぐ Viewed にする」を押したとき、ユーザーが現在表示しているタブに対してのみ処理を行うために使用します。 |
| `scripting` | 拡張機能をインストールまたは更新した直後、すでに開かれている GitHub のタブにはコンテンツスクリプトが読み込まれていません。ユーザーがポップアップの実行ボタンを押した場合に限り、そのタブに拡張機能パッケージ内のスクリプトを注入して処理を行うために使用します。外部から取得したコードは実行しません。 |
| `https://github.com/*`（ホスト権限） | GitHub の Pull Request ページの DOM からファイルパスと Viewed チェックボックスを読み取り、クリック操作を行うために必要です。GitHub 以外のサイトにはアクセスしません。 |

### リモートコードの使用

**「リモートコードを使用していません」** を選択します。

補足が求められた場合の説明:

```
すべてのコードは拡張機能パッケージ内に含まれています。manifest の web_accessible_resources には拡張機能自身の JavaScript ファイルを登録していますが、これは Manifest V3 のコンテンツスクリプトが ES modules を直接読み込めないため、パッケージ内の自分のモジュールを chrome.runtime.getURL() 経由で動的 import するためのものです。外部サーバーからコードを取得して実行することはありません。
```

### データ利用

- 収集するデータ: **なし**（すべての項目のチェックを外す）
- 3 つの証明にすべて同意:
  - 承認された用途以外でデータを使用または転送しない
  - 第三者に販売しない
  - 信用力の判断や融資目的で使用しない
- プライバシーポリシー URL: **不要**（機密性の高いユーザーデータを扱わないため）

## 4. 公開設定

- 公開設定: **限定公開**（Workspace ドメイン限定にする場合は **非公開** → ドメインを選択）
- 地域: 制限なし（限定公開では URL を知る人のみが対象なので、絞る必要はありません）
- 「審査のために送信」

審査は通常 1〜3 営業日程度です。承認後、ダッシュボードに表示される URL（`https://chromewebstore.google.com/detail/<拡張機能 ID>`）を社内で共有します。インストール後は自動更新が効きます。

## 5. 更新するとき

1. `manifest.json` の `version` を上げる
2. `npm run pack`
3. ダッシュボードの当該アイテム → 「パッケージ」→ 新しい zip をアップロード → 送信

更新時も審査があります（初回より短いことが多い）。

## 補足: `scripting` 権限を外す選択

`scripting` は「拡張機能を入れた直後に、すでに開いているタブで手動実行ボタンを使えるようにする」ためだけに使っています。申告する権限を減らしたい場合は次の変更で外せます。

1. `manifest.json` の `permissions` から `"scripting"` を削除
2. `src/popup/tab-bridge.js` の `sendMessage` から `chrome.scripting.executeScript` のフォールバックを削除し、失敗時はページの再読み込みを促すメッセージを出す

この場合、拡張機能を入れた直後は GitHub のタブを一度リロードする必要があります。通常の利用（ページを開いてからの自動実行）には影響しません。
